export const metadata = {
  title: 'Category Page',
  description: 'Browse Best Products By Categories',
}

export default function RootLayout({ children }) {
  return (
      <div>
        {children}
      </div>
  )
}
