export const metadata = {
    title: 'Register',
    description: 'Create An Account To Purchase From Zenmart.',
  }
  
  export default function RootLayout({ children }) {
    return (
        <div>
          {children}
        </div>
    )
  }
  