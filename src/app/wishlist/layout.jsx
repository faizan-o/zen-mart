export const metadata = {
    title: 'Your Wishlist',
    description: 'A Wishlist For Users.',
  }
  
  export default function RootLayout({ children }) {
    return (
        <div>
          {children}
        </div>
    )
  }
  